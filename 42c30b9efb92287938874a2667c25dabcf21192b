{
 "type": "CLEAR",
 "commit": "42c30b9efb92287938874a2667c25dabcf21192b",
 "when": "20260910-000507",
 "files": {
  ".githooks/adversary_audit.py": {
   "sha": "f9bf47136092e8d4710720edfbe33c7595ad4f1e7c1144dc9f8ae121b0fad4a0",
   "model": "z-ai/glm-5.3-flash",
   "when": "20260909-235925",
   "artifact": ".adversary\\reviews\\gate_20260909-235925.md"
  }
 },
 "artifacts": {
  ".adversary\\reviews\\gate_20260909-235925.md": "model: z-ai/glm-5.3-flash | usage: {'prompt_tokens': 6490, 'completion_tokens': 19996, 'total_tokens': 26486, 'cost': 0.0102035, 'is_byok': False, 'prompt_tokens_details': {'cached_tokens': 6400, 'cache_write_tokens': 0, 'audio_tokens': 0, 'video_tokens': 0}, 'cost_details': {'upstream_inference_cost': 0.0102035, 'upstream_inference_prompt_cost': 0.0002055, 'upstream_inference_completions_cost': 0.009998}, 'completion_tokens_details': {'reasoning_tokens': 19128, 'image_tokens': 0, 'audio_tokens': 0}} | scrubbed: 0 | files: ['.githooks/adversary_audit.py']\n\n## Adversarial review of the ROOT-epoch / ROOT-baseline change\n\nI attempted to refute the change on every axis I could construct. Findings from the hunt:\n\n**1. ROOT epoch vs. shallow-history check (the 70835b6 claim).** The staged bytes run `rev-parse --is-shallow-repository` *before* the `if sha == \"ROOT\": return sha, \"ok\"` early return, so a shallow clone with a `hook-names ROOT` epoch still returns `\"unreadable\"` \u2192 violation row in `main` + hook rule applied everywhere. The comment (\"ROOT skips ancestry checks, never the shallow-history check\") matches the bytes. No hole.\n\n**2. ROOT epoch as an escape hatch.** `_hook_names_apply` returns `True` (rule *applies*) for `sha == \"ROOT\"` \u2014 the strict branch, not the lax one. Converting a sha epoch \u2192 ROOT makes the rule apply everywhere (stricter); converting ROOT \u2192 a real sha is caught by the pre-existing ancestry check (the recorded sha cannot be an ancestor of the root commit that added `hook-names ROOT`, so state becomes `\"moved\"` \u2192 fail closed). No grandfathering vector.\n\n**3. Unborn-HEAD exit 0 (the 3c1243a claim).** The exit-0 path requires *all* of: `rev-list HEAD` failed, `symbolic-ref -q HEAD` succeeded, `show-ref --verify --quiet <branch>` returned exactly 1 (ref absent \u2014 documented git exit code), and `cat-file --batch-all-objects --batch-check=%(objecttype)` shows zero `commit` objects. A deleted branch ref with surviving (unreachable) commits, or history reachable from any other ref/tag/notes ref, yields `commit` lines \u2192 fail closed with exit 2. A missing/corrupt object yields a `<sha> missing` line \u2192 not in `{blob, tree, tag}` \u2192 fail closed. Detached/broken HEAD \u2192 `symbolic-ref -q` fails \u2192 fail closed. `all()` over an empty store is correctly `True` (truly nothing to audit). Notes-ref commit objects also force fail closed. Sound.\n\n**4. Control flow / regression on the non-ROOT path.** The `else:` branch is byte-identical behavior to the old code (rev-parse verify \u2192 rev-list with `check=True`). In the ROOT branch, the `rc != 0` block always returns (0 or 2), so fall-through to `revs = ...` only happens with a valid `out`. JSON document keys match the normal path exactly (`baseline`/`commits_walked`/`code_commits_audited`/`violations`/`overrides`).\n\n**5. Sentinel collisions.** `baseline == \"ROOT\"` is an exact match on installer-written content; a stray `root`/` ROOT` fails via `rev-parse` (exit 2, fail closed). A hand-written `hook-names ROOT` in a non-armed repo only *tightens* the hook rule. Epoch lines with extra tokens are skipped \u2192 `\"none\"` \u2192 fail closed.\n\n**6. Callers/contract.** Exit-code contract (0/1/2) preserved for every pre-existing input; the ROOT sentinel only activates when the new installer wrote it, so old baselines (real shas) behave identically. `audit_selftest` parity surface (CODE_EXTS/GATED_PREFIXES) untouched. Mid-walk git failures still raise `SystemExit` (exit 1) \u2014 pre-existing, unchanged by this diff, and fails closed (blocking) anyway.\n\n**7. Edge states probed and found conservative, not wrong:** unborn HEAD with other refs populated \u2192 exit 2 (documented choice); tag-only object store \u2192 nothing to audit, exit 0 is correct; grafts/replace tricks are pre-existing exposures not worsened (GIT_NO_REPLACE_OBJECTS set; replace-independent).\n\nNo defect introduced or enabled that I can trigger. The two fail-closed claims in the rebuttal are verified against the staged bytes, not just taken on faith.\n\nVERDICT: CLEAR"
 }
}
